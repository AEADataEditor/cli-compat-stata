*! version 1.1 16apr2024
program define window 
// do nothing
  di in green "This command is placeholder for a built-in command that "
  di in green " only exists in GUI versions (for some reason)."
  di in green "Ignoring it."
end
